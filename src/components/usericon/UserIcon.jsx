import React from "react";
import "./style.scss";
const UserIcon = (prop) => {
  return <span className="rounded-full border-1  px-[10px] py-[7px] UserIcon">{prop.latter}</span>;
};

export default UserIcon;
